# Upper bounds because the sweep runs unattended every four hours and pays per
# ticket: a major release landing mid-pilot would first be noticed by a live run.
anthropic>=0.92,<1
requests>=2.31,<3
# Test-only, but listed here because CI installs this file before running the
# tests, and the workflow-invariant test must actually run there.
PyYAML>=6,<7
